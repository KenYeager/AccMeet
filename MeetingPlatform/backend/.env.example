# Copy this file to .env and fill in your values
# (Not needed when running via docker compose — env vars are set in docker-compose.yml)

MONGO_URI=mongodb://localhost:27017/meetingplatform
CORS_ORIGINS=http://localhost:3000
